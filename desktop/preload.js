// Intentionally empty. Dory is a plain web app loaded from the deployed origin, so the
// renderer needs no Node/IPC bridge. Kept (with contextIsolation) for safe hardening
// and as the place to add a bridge later if the desktop app ever needs native features.
