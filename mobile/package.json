{
  "name": "dory-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor iOS shell for Dory. Wraps the live web app (see capacitor.config.json server.url).",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "build:sim": "xcodebuild -project ios/App/App.xcodeproj -scheme App -sdk iphonesimulator -derivedDataPath ios/App/build build"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.5.0"
  }
}
